import React from 'react'
import './Toolbar.css'

const toolbar = (props) => (
  <header className='Toolbar'>
    <div>MENU</div>
    <div>LOGO</div>
    <nav>
      <ul>
        
      </ul>
    </nav>
  </header>
)

export default toolbar